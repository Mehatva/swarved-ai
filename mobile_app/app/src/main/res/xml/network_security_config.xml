<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- MUST VERIFY: 10.0.2.2 is the Android Emulator's host-loopback address. Replace this
         single host for a physical-device demo; do not enable cleartext traffic globally. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
